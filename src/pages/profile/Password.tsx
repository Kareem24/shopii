import React from 'react';
import ResetForm from '../../components/ResetForm';

function Password() {
  return <ResetForm btnName={'change password'} className={''} btnClass={''} />;
}

export default Password;
